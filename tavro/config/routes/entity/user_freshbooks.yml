api_get_user_freshbooks:
    path: /users/{id}/freshbooks.{_format}
    defaults: { _controller: TavroApiBundle:Entity\UserQuickbooks:get, _format: json, entity: "user_freshbooks" }
    methods: [GET|OPTIONS]
    requirements:
        id: \d+
        _format:  json|xml

api_post_user_freshbooks:
    path:  /users/{id}/freshbooks.{_format}
    defaults: { _controller: TavroApiBundle:Entity\UserQuickbooks:post, _format: json, entity: "user_freshbooks" }
    methods: [POST|OPTIONS]
    requirements:
        _format:  json|xml

api_patch_user_freshbooks:
    path:  /users/{id}/freshbooks.{_format}
    defaults: { _controller: TavroApiBundle:Entity\UserQuickbooks:patch, _format: json, entity: "user_freshbooks" }
    methods: [PATCH|OPTIONS]
    requirements:
        id: \d+
        _format:  json|xml

api_put_user_freshbooks:
    path:  /users/{id}/freshbooks.{_format}
    defaults: { _controller: TavroApiBundle:Entity\UserQuickbooks:put, _format: json, entity: "user_freshbooks" }
    methods: [PUT|OPTIONS]
    requirements:
        id: \d+
        _format:  json|xml

api_delete_user_freshbooks:
    path:  /users/{id}/freshbooks.{_format}
    defaults: { _controller: TavroApiBundle:Entity\UserQuickbooks:delete, _format: json, entity: "user_freshbooks" }
    methods: [DELETE|OPTIONS]
    requirements:
        id: \d+
        _format:  json|xml