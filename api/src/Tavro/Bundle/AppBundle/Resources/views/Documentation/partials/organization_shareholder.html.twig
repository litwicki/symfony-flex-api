<div id="organization-shareholder">

    <div class="page-header">
        <h2>Organization Shareholder Api</h2>
        <p class="lead"><i class="fa fa-fw fa-cube"></i> Interact with <code>api.tavro.io</code> and manage OrganizationShareholder relationships.</p>
    </div>

    <div class="page-header">
        <h3>OrganizationShareholder Model</h3>
        <p>The Organization:Shareholder object model.</p>
    </div>

    <div class="panel panel-default">
        <table class="table table-striped table-hover table-api">
            <thead>
            <tr>
                <th>Field</th>
                <th>Type</th>
                <th>Methods</th>
                <th>Notes</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>id</td>
                <td class="integer">integer</td>
                <td>GET, DELETE</td>
                <td>Unique identifier.</td>
            </tr>
            <tr>
                <td>guid</td>
                <td class="string">string</td>
                <td>GET</td>
                <td>Globally unique identifier.</td>
            </tr>
            <tr>
                <td>update_date</td>
                <td class="date">date</td>
                <td>GET</td>
                <td></td>
            </tr>
            <tr>
                <td>create_date</td>
                <td class="date">date</td>
                <td>GET</td>
                <td></td>
            </tr>
            <tr>
                <td>organization <code>organization_id</code></td>
                <td class="integer">integer</td>
                <td>GET</td>
                <td>The unique identifier for the Organization object.</td>
            </tr>
            <tr>
                <td>shareholder <code>shareholder_id</code></td>
                <td class="integer">integer</td>
                <td>GET</td>
                <td>The unique identifier for the Shareholder object.</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="page-header">
        <h3><i class="fa fa-fw fa-location-arrow"></i> OrganizationShareholder Api Routes</h3>
    </div>

    <ul>
        <li><code>{organization_shareholder}</code> must be the integer ID of a OrganizationShareholder object.</li>
        <li><code>{format}</code> must be <code>XML</code> or <code>JSON</code> (if not provided the default is <code>JSON</code>).</li>
    </ul>

    <hr/>

    <div class="panel panel-default">
        <table class="table table-striped table-hover table-api">
            <thead>
            <tr>
                <th>Path</th>
                <th>Method(s)</th>
                <th>Description</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td class="code">/api/v1/organization_shareholders/{organization_shareholder}.{format}</td>
                <td class="code delete">DELETE</td>
                <td>Delete a OrganizationShareholder (remove a Organization from an Shareholder).</td>
            </tr>
            <tr>
                <td class="code">/api/v1/organization_shareholders.{format}</td>
                <td class="code post">POST</td>
                <td>Create a OrganizationShareholder (add a Organization to an Shareholder).</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="page-header">
        <h3>Example</h3>
        <p>An example request to add a Organization to an Shareholder.</p>
    </div>

    <!-- Nav tabs -->
    <ul class="nav nav-pills" role="tablist">
        <li role="presentation" class="active"><a href="#angular" aria-controls="angular" role="tab" data-toggle="tab">Angular</a></li>
        <li role="presentation"><a href="#php" aria-controls="php" role="tab" data-toggle="tab">PHP</a></li>
    </ul>

    <!-- Tab panes -->
    <div class="tab-content" style="margin-top: 2em;">
        <div role="tabpanel" class="tab-pane active" id="angular">
<pre><code class="javascript">var auth = Base64.encode({API_KEY} + ':' + {API_PASSWORD});
$http.defaults.headers.common['Authorization'] = 'Basic ' + auth;

var organization_id = 890;
var shareholder_id = 123;

var obj = {
    organization: organization_id,
    shareholder: shareholder_id
};

var url = 'https://{{ api_hostname }}/api/v1/organization_shareholders';

$http.post(url, obj)
.success(function(data) {
    alert('Organization was added to this Shareholder!');
});</code></pre>
        </div>
        <div role="tabpanel" class="tab-pane" id="php">
<pre><code class="php">$url = 'https://{{ api_hostname }}/api/v1/organization_shareholders';

$curl = new Curl();
$curl->setBasicAuthentication('{API_KEY}', '{API_PASSWORD}');
$curl->post($url, array(
    'organization' => 890,
    'shareholder' => 123
));

if ($curl->error) {
    echo 'Error: ' . $curl->errorCode . ': ' . $curl->errorMessage;
}
else {
    echo $curl->response;
}</code></pre>
        </div>
    </div>

</div>