twig:
    paths: ['%kernel.project_dir%/templates']
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    cache:  false
    globals:
        database_url: "%env(DATABASE_URL)%"